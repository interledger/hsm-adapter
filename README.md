# hsm-adapter
The Interledger Protobuf for providing HSM integration.

# Lint
Run the following `buf` (install via brew: `brew install buf`) command to validate the protobuf:
`buf build`.

# CI
Please see: `.github/workflows`
